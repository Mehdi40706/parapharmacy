import { defineComponent, computed, mergeProps, unref, useSSRContext } from "vue";
import { ssrRenderAttrs, ssrInterpolate } from "vue/server-renderer";
import { u as useApi } from "./useApi-BHrVqC1i.js";
const _sfc_main = /* @__PURE__ */ defineComponent({
  __name: "OrderStatusBadge",
  __ssrInlineRender: true,
  props: {
    status: {}
  },
  setup(__props) {
    const props = __props;
    const map = {
      PENDING: { label: "En attente", color: "bg-honey/10 text-honey-dark" },
      CONFIRMED: { label: "Confirmée", color: "bg-sage/10 text-sage-dark" },
      PROCESSING: { label: "En préparation", color: "bg-sage/10 text-sage-dark" },
      SHIPPED: { label: "Expédiée", color: "bg-sage/10 text-sage-dark" },
      DELIVERED: { label: "Livrée", color: "bg-sage text-white" },
      CANCELLED: { label: "Annulée", color: "bg-clay/10 text-clay" }
    };
    const label = computed(() => map[props.status]?.label || props.status);
    const colorClass = computed(() => map[props.status]?.color || "bg-mist text-ink/60");
    return (_ctx, _push, _parent, _attrs) => {
      _push(`<span${ssrRenderAttrs(mergeProps({
        class: ["badge-pill", unref(colorClass)]
      }, _attrs))}>${ssrInterpolate(unref(label))}</span>`);
    };
  }
});
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("components/order/OrderStatusBadge.vue");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const __nuxt_component_0 = Object.assign(_sfc_main, { __name: "OrderStatusBadge" });
const normalizeOrder = (o) => ({
  ...o,
  totalPrice: Number(o.totalPrice),
  items: o.items.map((item) => ({
    ...item,
    price: Number(item.price),
    product: { ...item.product, price: Number(item.product.price) }
  }))
});
const useAdminOrders = () => {
  const api = useApi();
  const fetchAllOrders = async (query) => {
    const result = await api("/orders", { params: query });
    return { ...result, data: result.data.map(normalizeOrder) };
  };
  const updateOrderStatus = (id, status) => api(`/orders/${id}/status`, { method: "PATCH", body: { status } });
  return { fetchAllOrders, updateOrderStatus };
};
export {
  __nuxt_component_0 as _,
  useAdminOrders as u
};
//# sourceMappingURL=useAdminOrders-BfKYx_06.js.map
